plugins {
    id 'application'
    id 'rabbitmq-work-queues-sample.java-conventions'
}

application {
    mainClass = 'br.com.globalbyte.samples.rabbit.wq.NewTask'
}

jar {
    manifest {
        attributes "Main-Class": "br.com.globalbyte.samples.rabbit.wq.NewTask"
    }

    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}